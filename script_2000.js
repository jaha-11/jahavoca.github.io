let quiz=quizData_2000.slice(0,500);let current=0,score=0,timer=5,paused=false,timerInterval;function shuffle(a){for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;}function togglePause(){paused=!paused;document.getElementById("pauseBtn").innerText=paused?"▶️":"⏸";}function checkAnswer(selected,correct){clearInterval(timerInterval);document.querySelectorAll(".choice").forEach(b=>{if(b.innerText==correct)b.classList.add("correct");if(b.innerText==selected&&selected!=correct)b.classList.add("incorrect");});if(selected==correct)score++;setTimeout(()=>{current++;loadQuestion();},1000);}function markAnswer(_,correct){document.querySelectorAll(".choice").forEach(b=>{if(b.innerText==correct)b.classList.add("correct");});setTimeout(()=>{current++;loadQuestion();},1000);}function updateTimer(){document.getElementById("timer").innerText=timer;}function loadQuestion(){if(current>=quiz.length){document.body.innerHTML=`<h2>퀴즈 완료<br>${score}/${quiz.length}</h2>`;return;}let q=quiz[current];document.getElementById("counter").innerText=`${current+1}/500`;document.getElementById("question").innerText=q.word;let options=[q.meaning];while(options.length<4){let m=quiz[Math.floor(Math.random()*quiz.length)].meaning;if(!options.includes(m))options.push(m);}shuffle(options);let cEl=document.getElementById("choices");cEl.innerHTML="";options.forEach(opt=>{let btn=document.createElement("div");btn.className="choice";btn.innerText=opt;btn.onclick=()=>checkAnswer(opt,q.meaning);cEl.appendChild(btn);});timer=5;updateTimer();clearInterval(timerInterval);timerInterval=setInterval(()=>{if(!paused){timer--;updateTimer();if(timer===0){clearInterval(timerInterval);markAnswer(null,q.meaning);}}},1000);}loadQuestion();